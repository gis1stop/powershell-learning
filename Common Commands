# check powershell version
The installation package for PowerShell comes inside a WMF installer. The version of the WMF installer matches the version of PowerShell:
Run $PSVersionTable.PSVersion to get the version.
